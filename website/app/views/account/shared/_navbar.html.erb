<nav class="navbar navbar-fixed-top navbar-dark bg-primary">
  <button class="navbar-toggler hidden-sm-up pull-right" type="button" data-toggle="collapse" data-target="#collapsingNavbar">
    ☰
  </button>
  <a class="navbar-brand" href="#">SSH Bastion</a>
  <div class="collapse navbar-toggleable-xs" id="collapsingNavbar">
    <ul class="nav navbar-nav pull-right">
      <% if account_signed_in? %>

      <li class="nav-item active">
        <a class="nav-link" href="<%= account_root_path %>"><%= t('view.navbar.dashboard', default: 'Dashboard') %></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/account/profile"><%= t('view.navbar.profile', default: 'Profile') %></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="<%= destroy_account_session_path %>" data-method="delete"><%= t('view.navbar.sign_out', default: 'Sign out')%></a>
      </li>
      <% else %>
      <li class="nav-item">
        <a class="nav-link" href="<%= new_account_session_path %>"><%= t('view.navbar.sign_in', default: 'Sign in')%></a>
      </li>
      <% end %>
    </ul>
  </div>
</nav>

