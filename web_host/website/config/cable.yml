# Action Cable uses Redis by default to administer connections, channels, and sending/receiving messages over the WebSocket.
production:
  adapter: redis
  url: redis://<%= Settings.web_redis.host %>:<%= Settings.web_redis.port %>/1

development:
  adapter: async

test:
  adapter: async
